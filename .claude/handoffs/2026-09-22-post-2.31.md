## Handoff: dev-skills post-2.31.0

**Goal:** Choose the next piece of work. v2.30.0 and v2.31.0 have both shipped.

**Current state:**
- **v2.30.0** (tag on `3a0d0ea`, PR #56):
  - one-call session-start probe
  - one-call session-end evidence
  - chaining rule applies in every mode
  - compact tracker and banner
  - fixed `validate.sh` failing at random under pipefail
- **v2.31.0** (tag on `d9cd5b7`, PR #57):
  - `SKILL.md` down from 54.6KB to 40.6KB
  - `scripts/rule-phrases.txt`: 53 rule phrases that `validate.sh` enforces
  - `SKILL.md` size ceiling 44KB
  - release-track contradiction fixed
  - full README pass
- For both releases, the release asset has the same sha256 as the committed bundle.
- The gate file's SHIP ✅ line for 2.31.0 is held locally and rides in the next
  release's PR.

**Gate status:** v2.31.0 is complete. The next version starts with all gates ⬜.
```
🔢✅ 🔨✅ 🔒✅ 📄✅ 📦✅ 🚀✅ (v2.31.0)
```

**Mode:** semi-autonomous this session. The next session asks again.

**Key files:**
- `scripts/rule-phrases.txt`: removing a line removes a rule. Say so in the CHANGELOG.
- `scripts/validate.sh`: rule phrases, size ceilings (SKILL.md 44KB), and a
  check that the probe and the self-check list the same reference files.
- `skills/dev-skills/SESSION_START.md`: the probe block (top) and the gate-file
  format ("Write the gate state file").
- `skills/dev-skills/GATE_REFERENCE.md`: "Gate state file", which holds the
  re-derivation table and the resume procedure.
- `hooks/gate-preflight.sh:135-146`: which gates each operation requires.

**Decisions made:**
- Rules and triggers stay in `SKILL.md`. Formats and procedures go to the file
  that's already loaded when they're needed.
- Section numbers are stable, because other files point at them.
- shellcheck isn't installed on this host. Use a throwaway venv:
  `pip install shellcheck-py==0.11.0.1`.

**Open items:**
1. **Hook vs. prose (2.31.1 candidate):** `gh pr merge` requires Version,
   Build, Security, Docs and Release in the hook. The written rules let a
   bookkeeping merge mark only Release and Ship ➖ N/A. Decide which one wins.
2. **Installed skill:** `~/.claude-steve/skills/dev-skills/` is still v2.29.0.
   Re-unzip v2.31.0 there.
3. **Merged branches on the remote** (deleting them is yours):
   `feature/skill-md-trim`, `feature/cost-efficiency-pass`,
   `docs/handoff-cost-efficiency`, and about 12 older ones.
4. **Next extraction target:** `WORKFLOW_REFERENCE.md` (~40KB, ceiling 44KB).
   Its best-practices and Dependabot sections are the next thing to split out.

**Shell environment:** Linux bash, local native Linux (Debian 13)

**Next step:** Update the installed skill to v2.31.0, then start a fresh
session and decide open item 1.
