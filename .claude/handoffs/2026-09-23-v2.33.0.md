## Handoff: dev-skills v2.32.0 + v2.33.0 (claude-vibe-skills)

**Goal:** Improve dev-skills session start, merge safety, and self-update.

**Current state:** Both releases shipped and verified. v2.32.0 (tag → 18d4246)
and v2.33.0 (tag → 17a8bd2) are published with `dev-skills.skill` attached.
PRs #58 and #59 are merged. Nothing pending.

**Gate status (v2.33.0):**
```
🔢 VERSION    ✅
🔨 BUILD      ✅
🔒 SECURITY   ✅ 0 open
📄 DOCS       ✅
📦 RELEASE    ✅ PR #59
🚀 SHIP       ✅ tag at 17a8bd2, release run ✅, asset verified
```
`.claude/dev-skills-gates.md` records SHIP ✅ for v2.33.0 (added in the same
commit as this handoff).

**Mode:** this session ran semi-autonomous; the next session asks again.

**Key files:**
- `skills/dev-skills/SESSION_START.md`: mode picker with a `Cost:` line per
  option (exact wording per environment); one `/mcp disable <server>` line per
  active server; three-way update offer when the copy is outdated
- `skills/dev-skills/UPDATE_REFERENCE.md`: install procedure (pinned release
  asset, whole-directory replace, version check first, refuses any target not
  named `dev-skills`)
- `skills/dev-skills/GATE_REFERENCE.md`, Gate 2 "Test artifact before merge":
  artifact from the merged commit required in every environment; a fresh
  throwaway Docker login each run, bound to `127.0.0.1`
- `hooks/gate-preflight.sh`: denies merges without `test artifact:` (and
  `test creds` in Docker/compose repos) on the BUILD row

**Decisions made:**
- The mode picker stays neutral: cost lines describe how each mode processes,
  with no recommendation
- Remote and Termux sessions get test artifacts from CI (PR artifact or dev
  pre-release); `handoff n/a` is no longer valid before a merge
- `SESSION_START.md` is at its 32KB ceiling: new procedures go into on-demand
  reference files

**Open items:**
- The PowerShell install block in `UPDATE_REFERENCE.md` is untested (no `pwsh`
  in the container); test it on Windows
- `shellcheck` was not run locally on `gate-preflight.sh`; CI linted it

**Shell environment:** container bash (remote); tag blocks go to the user.

**Next step:** Reinstall dev-skills from the v2.33.0 release, then start the
next session on that version.
